# Variáveis de ambiente para o ambiente de desenvolvimento local (Docker)
# Este arquivo NÃO deve ser commitado.

# Aponta para o container do PostgreSQL/PostGIS no Docker
POSTGRES_POSTGRES_URL=postgresql://user:password@localhost:5432/precoreal_geo

# Aponta para o container do Kafka no Docker
KAFKA_BOOTSTRAP_SERVER=localhost:9092

# As variáveis de Firebase e outras chaves de API (KAFKA_API_KEY, etc.) são omitidas intencionalmente.
# A lógica da aplicação foi ajustada para usar uma configuração diferente (sem SASL) para o Kafka quando KAFKA_API_KEY não está presente.
# Para funcionalidades que dependem do Firebase, você pode adicionar a variável FIREBASE_ADMIN_SDK_BASE64 aqui se tiver uma conta de serviço para desenvolvimento.
