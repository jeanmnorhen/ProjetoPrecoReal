# Variáveis para o ambiente local (Docker) do servico-monitoramento

# Aponta para o container do Kafka no Docker
KAFKA_BOOTSTRAP_SERVER=localhost:9092

# A variável CRON_SECRET é usada para proteger endpoints de consumo. Defina um valor para testes locais.
CRON_SECRET=um-segredo-local-super-secreto

# O InfluxDB não está no docker-compose.yml. 
# Se você tiver uma instância local, descomente e preencha as variáveis abaixo.
# INFLUXDB_URL=http://localhost:8086
# INFLUXDB_TOKEN=seu-token-local
# INFLUXDB_ORG=sua-org-local
# INFLUXDB_BUCKET=seu-bucket-local
