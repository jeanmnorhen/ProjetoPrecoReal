# Variáveis para o ambiente local (Docker) do servico_agentes_ia

# Aponta para o container do Kafka no Docker
KAFKA_BOOTSTRAP_SERVER=localhost:9092

# A variável CRON_SECRET é usada para proteger endpoints de consumo. Defina um valor para testes locais.
CRON_SECRET=um-segredo-local-super-secreto

# Para funcionalidades que dependem do Gemini, você pode adicionar a variável GEMINI_API_KEY aqui.
# GEMINI_API_KEY=sua-chave-api-gemini

# Para funcionalidades que dependem do Firebase, você pode adicionar a variável FIREBASE_ADMIN_SDK_BASE64 aqui.
# FIREBASE_ADMIN_SDK_BASE64=seu-sdk-em-base64
